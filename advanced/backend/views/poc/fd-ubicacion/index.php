<?php

use yii\helpers\Html;
use yii\helpers\Url;
use yii\grid\GridView;

/* @var $this yii\web\View */
/* @var $searchModel common\models\poc\FdUbicacionSearch */
/* @var $dataProvider yii\data\ActiveDataProvider */

$this->title = 'Fd Ubicacions';
$this->params['breadcrumbs'][] = $this->title;
?>
<div class="fd-ubicacion-index">

    <h1><?= Html::encode($this->title) ?></h1>
    <?php // echo $this->render('_search', ['model' => $searchModel]); ?>

    <p>
        <?= Html::button('Create Fd Ubicacion', 
        ['value' =>Url::to(['poc/fd-ubicacion/create']), 'title' => 'Create Fd Ubicacion',
        'class' => 'showModalButton btn btn-success']); ?>
    </p>
    
    <?= GridView::widget([
        'dataProvider' => $dataProvider,
        'filterModel' => $searchModel,
        'columns' => [
            ['class' => 'yii\grid\SerialColumn'],

            'id_ubicacion',
            'cod_parroquia',
            'cod_canton',
            'cod_provincia',
            'id_demarcacion',
            // 'cod_centro_atencion_ciudadano',
            // 'descripcion_ubicacion',
            // 'id_conjunto_respuesta',
            // 'id_pregunta',
            // 'id_respuesta',

            [
			
                'class' => 'yii\grid\ActionColumn',
                'header' => 'Action',
                'template' => ' {view} {update} {delete}',
                'buttons' => [
                    'view' => function($url, $model){
                            return Html::a('<span class="glyphicon glyphicon-eye-open">Ver</span>',Yii::getAlias($url),[
                                    'title' => Yii::t('app', 'View'),
                                    'data-method' => 'post',
                            ]);
                    },
                    'update' => function ($url, $model) {
                            return Html::button('<span class="glyphicon glyphicon-pencil">Editar</span>',  ['value'=>$url,
                                         'class' => 'btn btn-default btn-xs showModalButton',
                            ]);
                    }, //Primera columna encontrada id_ubicacion                    
                    'delete' => function($url, $model){
                            $url2 = Url::toRoute(['poc/fd-ubicacion/deletep','id' => $model->id_ubicacion],true);
                            return Html::a('<span class="glyphicon glyphicon-trash">Borrar</span>',$url2,[
                                    'title' => Yii::t('app', 'Delete'),
                                    'data-method' => 'post',
                                    'data-confirm' => Yii::t('yii', 'Desea Eliminar el Regitro?'),
                            ]);
                    }
                                        
                                        
            ],
			
			
	],
        ],
    ]); ?>
</div>
